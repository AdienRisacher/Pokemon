{% extends 'base.html.twig' %}
{% block title %}
    {{ 'Inscription' }}
{% endblock %}

{%block stylesheets%}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/login_style.css') }}">
{%endblock%}




{% block body %}

<div class="main">

    <div class= "container b-container is-txl is-z200" id="b-container">
        {{ form_start(form, {'attr': {'class': 'form','id': 'a-form'}}) }}
        {{form_row(form.username, {'attr': {'class': 'form__input','placeholder': 'Username'}})}}
        {{form_row(form.email, {'attr': {'class': 'form__input','placeholder': 'Email'}})}}
        {{form_row(form.password, {'attr': {'class': 'form__input','placeholder': 'Password'}})}}
        {{form_row(form.repeat_password, {'attr': {'class': 'form__input','placeholder': 'ConfirmPassword'}})}}
        <button class="form__button button " type="submit">inscrire</button>
        {{ form_end(form) }}
   </div>

    <div class="switch is-txr" id="switch-cnt">
        <div class="switch__circle"></div>
        <div class="switch__circle switch__circle--t"></div>
        <div class="switch__container" id="switch-c1">
            <h2 class="switch__title title">Bonjour!</h2>
            <p class="switch__description description">Entrez vos données personnelles et commencez une adventure avec nous</p>
            <div class="link"><a href="{{path("app_login")}}"><span>connecter</span></a> </div>
        </div>
    </div>
</div>

{% endblock %}
{% block javascripts %}

{% endblock %}

